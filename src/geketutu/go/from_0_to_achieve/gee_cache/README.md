### 分布式缓存


#### day1-lru

FIFO 先进先出

LFU 最少使用

LRU 最近最少使用

kv := ele.Value.(*entry) 断言，将 ele.Value 转换成 entry类型

#### day2-单机并发缓存

Sync.Mutex 互斥锁


Group 核心数据结构，负责与用户的交互，并且控制缓存值存储和获取的流程

day3 - HTTP 服务端
如何构建Http server

day4 - 一致性哈希
提高缓存效率并且节约存储空间

一致性哈希算法在新增或删除节点时，只需要重新定位该节点附近的一小部分数据,而不是所有节点

为解决数据倾斜问题，引入虚节点，一个真实节点对应多个虚节点
